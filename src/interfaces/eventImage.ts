export interface EventData {
  title: string;
  seasonRound: string;
  date: string;
  classes: string;
  adrrWebsite: string;
  colour: string; // The primary color for the banner, e.g., 'rgb(255, 0, 0)'
  imagePath: string; // The URL or path to the main event image
}